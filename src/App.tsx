import * as React from 'react';
import NavegadorStack from './navigator/NavegadorStack';

const App = () => {
  return (
    // Navegador Principal, por ahora solo el stack
    <NavegadorStack />
  );
};

export default App;
